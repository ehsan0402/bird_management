spring:
  datasource:
    url: jdbc:tc:mariadb://localhost:3306/birdie
    username: root
    password: birdie
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: validate